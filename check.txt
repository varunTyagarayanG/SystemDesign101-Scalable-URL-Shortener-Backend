docker-compose exec mongo mongosh --quiet --eval "use urlshortener; show collections;"
# You should see "urls" and once analytics run, also "events" and "users" (after registering).

docker-compose exec mongo mongosh --quiet --eval "use urlshortener; db.urls.count();"
# After you create at least one URL, this returns 1 or more.

docker-compose exec mongo mongosh --quiet --eval "use urlshortener; db.events.count();"
# After you do a redirect or two, this returns a positive number.



http://localhost:3000/metrics